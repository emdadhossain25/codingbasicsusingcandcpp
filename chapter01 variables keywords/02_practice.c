#include <stdio.h>

int main()
{
    int radius = 3;
    float pie = 3.14;
    printf("the area of this circle is %f\n", pie * radius * radius);
    int height = 3;
    printf("the volume of this cylinder is %f\n", pie * radius * radius * height);

    return 0;
}